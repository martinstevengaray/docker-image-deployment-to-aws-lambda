plugins {
    // Lets Gradle auto-download a matching JDK for the Java toolchain (e.g. JDK 21).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'docker-image-deployment-to-aws-lambda'
